# 《史鉴》历史学习智能体（统一云端版）

《史鉴》是一个可运行的历史学习智能体原型。它把一次学习任务拆成：

`识别问题 → 规划任务 → 检索史料 → 组织回答 → 核验引用 → 输出反馈 → 记录薄弱点`

三人团队通过一个统一网站协作：建议、史料、项目版本和（配置后）真实 AI 请求都由服务器管理，成员不需要反复互发 ZIP。

## 先用统一入口

推荐由成员 2 在一台电脑双击 `启动史鉴团队网站.bat`，然后三个人访问服务器窗口显示的：

`http://主机地址:8772/史鉴团队网站.html`

入口包含历史智能体、团队建议中心和使用说明。页面通过服务器自动读取云端史料和 AI 配置。修改服务器目录里的网页或服务文件后，其他成员刷新同一网址即可看到新版本。

- `127.0.0.1` 只能本机访问；发给队友应使用窗口显示的局域网地址。
- 局域网地址要求设备连接同一网络。
- 校外公开访问需要进一步部署公网服务器、HTTPS、登录/邀请码、权限、限流和备份。

## 智能体能做什么

- **证据问答**：从已整理的史料中检索，输出回答、时间线、来源和事实/推断/争议标签；
- **材料题训练**：生成题目，并按观点、证据、解释、史实准确性四项反馈；
- **史料知识库**：管理员在云端管理页发布成员 3 核验过的资料；
- **可追溯输出**：关键事实必须绑定来源；找不到证据时明确写“资料不足”，不能自行补出处；
- **离线演示**：未连接真实模型时仍可演示固定流程，但页面明确标注离线规则。

## 云端管理

管理员打开：

`http://主机地址:8772/史鉴云端管理.html`

输入服务器的 `SHIJIAN_ADMIN_TOKEN` 后，可以修改系统提示词、项目版本和云端史料。成员 3 先核验资料，再点击“保存全部史料”；其他人刷新统一入口即可看到。

系统会把数据保存在服务器目录的 `史鉴云端.db`。该数据库是运行时数据，不要放入项目 ZIP，也不要把它提交到公开仓库；部署前应另行备份。

## 接入真实 AI

统一服务器通过环境变量保存密钥，并向 OpenAI 兼容接口转发请求。启动前设置：

```powershell
$env:SHIJIAN_BASE_URL = "https://你的中转站/v1"
$env:SHIJIAN_API_KEY = "你的密钥"
$env:SHIJIAN_MODEL = "你的模型名"
$env:SHIJIAN_ADMIN_TOKEN = "自行设置的管理员令牌"
python .\史鉴统一网站_server.py
```

服务器会把网页请求转发到 `/api/v1/chat/completions`，浏览器不接触真实密钥。未配置 `BASE_URL`、`API_KEY` 或 `MODEL` 时，接口状态会显示未配置，智能体自动保留离线演示。

不要把密钥写入 HTML、提交到 Git、发到群里或放进课程压缩包。

## 反馈与同步

在统一入口打开“团队建议中心”。建议、史料纠错、模型错误、界面要求和验收标准会写入共享数据库；页面会自动拉取最新列表。建议需要填写场景、当前问题、证据、希望结果和验收标准，提交后按 P0/P1/P2 排序。

在线共享模式不再依赖“导出 JSON 再发群”。页面保留导出功能只用于离线备份或无网络协作；如果服务器暂时不可用，网页会退回本机模式，恢复连接后再提交到共享看板。

## 资料格式

管理员发布史料时，每条资料至少包括：

```json
{
  "id": "S001",
  "title": "资料标题",
  "author": "作者或机构",
  "date": "时间或版本",
  "period": "历史时期",
  "locator": "页码或链接",
  "content": "原文或整理内容",
  "reliability": "教材/官方|权威出版物|待核验"
}
```

正式课程版本还应补充教材页码、原文摘录、链接、核验人和版本日期。成员 3 对史实负责，成员 2 验证运行，成员 1 记录输出、错误与修复。任何 AI 生成内容都必须经过人工核验。

## 备用与边界

`史鉴智能体.html` 和 `史鉴建议中心.html` 可以单独打开做离线演示，但从文件直接打开时无法访问云端 API；正式协作应使用统一服务器入口。旧的单功能代理脚本不是统一网站的启动入口，除非成员 2 明确进行本地排障，不要让团队成员分别启动多个服务。

项目包不包含 API 密钥和运行中的 SQLite 数据库。更新程序时由成员 2 备份数据库、替换代码文件、重启服务器并通知成员刷新；不要删除 `史鉴云端.db`，否则会丢失共享建议和史料。

## 团队角色

- **成员 1（产品与测试）**：需求优先级、固定测试集、验收和汇报；
- **成员 2（技术与部署）**：服务器、AI 接口、版本和现场演示；
- **成员 3（历史与史料）**：范围、来源、标准答案和争议核验；
- **Codex / GPT-6 Astra（AI 主教练）**：架构、程序、提示词、测试、文档和答辩辅导，不占三人名额。

三位成员负责理解、运行、史实核验和最终判断；不得伪造测试数据、引用或用户反馈。
