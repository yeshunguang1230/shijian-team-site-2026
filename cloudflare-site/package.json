{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && node tests/smoke.mjs",
    "check:e2e": "node tests/d1-e2e.mjs",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply shijian-team-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply shijian-team-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  }
}
